# {{ ansible_managed }}

set v15-compat
{% if smtp_from is defined and smtp_from %}
set from="{{ smtp_from }}"
{% endif %}
{% if smtp_server_address is defined and smtp_server_address and smtp_server_port is defined and smtp_server_port and smtp_user is defined and smtp_user and smtp_password is defined and smtp_password %}
set mta=smtp://{{ smtp_user }}:{{ smtp_password }}@{{ smtp_server_address }}:{{ smtp_server_port }} smtp-use-starttls
{% endif %}
