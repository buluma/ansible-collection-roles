{{ ansible_managed | comment }}

{% for host in postgresql_replication__other_nodes %}
{{ hostvars[host].ansible_default_ipv4.address }}:5432:replication:{{ postgresql_replication__user }}:{{ postgresql_replication__password }}
{% endfor %}
