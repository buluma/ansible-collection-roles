galaxy_info:
  author: buluma
  role_name: ubuntu_setup
  description: Setup and configure fresh Ubuntu installation.
  company: ShadowNet
  license: Apache-2.0

  min_ansible_version: "2.1"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy

  galaxy_tags:
    - ubuntu
    - setup
    - configure

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
