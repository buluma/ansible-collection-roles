---
galaxy_info:
  author: buluma
  role_name: macos_setup
  description: Setup your macOS in quick simple steps using ansible.
  company: ShadowNet
  license: Apache-2.0

  min_ansible_version: "2.1"
  platforms:
    - name: Fedora
      versions:
        - all
    - name: MacOS
      versions:
        - all

  galaxy_tags:
    - macOS
    - setup
    - config

dependencies: []
