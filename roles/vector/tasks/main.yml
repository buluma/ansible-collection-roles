---

- name: install vector
  include: install.yml
  tags:
    - vector_install

- name: configure vector
  include: config.yml
  tags:
    - vector_configure

- name: service configuration
  include: service.yml
  tags:
    - vector_service
